# ===========================================
# Garibaldi Login Service Configuration
# ===========================================

# Database connection
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/auth?schema=public"

# Server port
PORT=3000

# JWT secret for token signing
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Redis connection (for session/token management)
REDIS_URL=redis://localhost:6379

# Portal URL (for redirects after auth)
PORTAL_BASE_URL=http://localhost:5173

# SMTP Configuration (for password reset emails, etc.)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=